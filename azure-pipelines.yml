trigger:
- none

pool:
  name: Poolmyagents
  demands:
    - agent.name -equals agentm
 

steps:

- task: FlutterBuild@0
  inputs:
    target: 'apk'
    projectDirectory: './'
    flutterDirectory: '/home/azureuser/myagent/sdk/flutter/bin'