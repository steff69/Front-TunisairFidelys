trigger:
- none

pool:
  name: bara
  demands:
    - agent.name -equals vmagentm
 

steps:

- task: FlutterBuild@0
  inputs:
    target: 'apk'
    projectDirectory: './'
    flutterDirectory: '/home/azureuser/myagent/sdk/flutter/bin'